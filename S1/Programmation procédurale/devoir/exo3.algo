Algorithme Moyenne;

Variable a,b,c: entier;
         max,moyenne:entier;

Debut

Ecrire("Veuillez saisir vos nombre A,B et C SVP :);
Lire(a,b,c);

Si (A>0 et B>0 et C>0) Alors 
    Debut 
    moyenne < (a+b+c)div3;
    Si (a<b) Alors max < a;
             Sinon max < b
    Si (max<c) Alors max < c;
    Ecrire("Moyenne";moyenne,"Maxilul",max);
    Fin
Sinon
    Ecrire("Vos nombre doit etre positifs");
fin;

