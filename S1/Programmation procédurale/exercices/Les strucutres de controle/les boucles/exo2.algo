Algorithme factorielle

Variable i,n,fact,

Debut

Ecrire("Veiullez entrer un nombre :");
Lire(n);


Si n < 1 Alors 
    Ecrire("Veiullez entrer un nombre positif.");

Sinon 
    fact < 1;
    pour i allant de 1 à n (pas=-1)
        Faire
            fact<*i;
    Ecrire("Le resultat est ",counter);
Fin;